import { StyleSheet, Text, View, Platform } from "react-native";
import React from "react";
import * as ImagePicker from "expo-image-picker";

export default function userImg({ setPicture }) {
  return (
    <View>
      <Text>userImg</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
